name: build dockerfile

inputs:
    MY_NAME:
        description: name
        required: true
        default: Phuoc

runs:
    using: "docker"
    image: "Dockerfile"

branding:
    icon: "mic"
    color: "red"
